继续排查 `go test -race ./...` 的失败原因，先重新跑一遍获取完整输出。
全量 race 测试发现一处真实数据竞争：`ws_client.go` 中 `writePump`(line 291) 与 `readPump`(line 380) 的两个 defer 并发写同一地址。读取该文件确认具体字段。
找到根因：`readPump`（line 377-382）与 `writePump`（line 287-294）两个 goroutine 的 defer 都在做 `ch.Conn.Close()` + `ch.Conn = nil`，并发写 `ch.Conn` 导致数据竞争。查看 `WsChannelClient` 定义以确定修复点。
修复方案：给 `WsChannelClient` 增加 `sync.Once` 保护的 `closeConn()`，两个 pump 的 defer 统一调用，确保 `Conn.Close()` 与置 nil 只执行一次。
现在实施修复。先改 `channel_client.go`，再改 `ws_client.go` 两个 pump 的 defer。同时检查服务端 `ws_server.go` 是否有同样模式。
服务端已通过 `ch.Close()`（`doneOnce` 幂等）统一收口，无此问题。现在修复客户端：给 `WsChannelClient` 加 `closeOnce` 保护的 `closeConn()`。
现在改 `ws_client.go` 中两个 pump 的 defer，统一调用 `ch.closeConn()`。
验证编译与测试。
全量 race 测试通过。再用 verbose 确认根目录测试用例全部执行，并多跑几轮验证稳定性。
全部 8 个测试用例（含 `-count=2` 重复运行）稳定通过。最后检查 lint。
全部修复完成，验证通过。总结本次收尾工作：

## 本次修复的最后一个数据竞争

**根因**：`nrpc/wsocket/ws_client.go` 中 `readPump` 与 `writePump` 两个 goroutine 的 defer 都会执行 `ch.Conn.Close()` + `ch.Conn = nil`，并发写 `WsChannelClient.Conn` 字段触发 race。

**修复**（`channel_client.go` + `ws_client.go`）：
- `WsChannelClient` 新增 `closeOnce sync.Once` 与幂等方法 `closeConn()`，保证底层连接只被关闭/置空一次
- `readPump`/`writePump` 的 defer 统一改调 `ch.closeConn()`，与外部 `Close()` 也通过 `closeOnce` 收口，与之前服务端 `WsChannelServer.Close()`（`doneOnce`）的修复模式一致

## 最终验证结果

| 检查项 | 结果 |
|---|---|
| `go build ./...` | 通过 |
| `go vet ./...` | 通过 |
| `go test -race ./...` | 全子包 `ok`，无 race 警告 |
| 根目录 `-v -count=2` 8 个用例 | 全部 PASS，重复运行稳定 |
| lint | 0 错误 |

## 整个会话累计成果

1. **稳定性**：消除 5 处真实数据竞争（`WsChannelServer` 字段清理、`LocalClient.client`、`ServerRpc.Listen`、`ClientRpc.Serve`、`WsChannelClient.Conn`），全部采用 `sync.RWMutex`/`sync.Once` + getter/setter 统一模式；`codec.go` 加长度保护、`conv.go` 防越界、`room.go` 广播免快照分配
2. **测试**：新增 `connect_test.go`（端到端 WS 集成、Origin/限额/未知协议/并发注册）、`codec_test.go`、`ref_test.go`
3. **多协议扩展准备**：`ProtocolFactory`/`nrpc.Transport` 抽象保留，TCP/KCP 限额字段预留，`Dial` 未知协议显式报错不再静默降级，`WithAutoBan*` 半成品字段移除